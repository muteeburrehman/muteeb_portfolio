# Muteeb Portfolio — standalone production stack
#
# Architecture:
#   Internet / Cloudflare → caddy (:80/:443) → web (:80 internal) → api (:8000 internal)
#
# Setup:
#   cp .env.example .env   # set SITE_DOMAIN, ACME_EMAIL, EMAIL_*
#   docker compose up -d --build
#
# Verify:
#   curl -sS https://dev.muteeblabs.uk/healthz   # → ok

name: muteeb-portfolio

services:
  caddy:
    image: caddy:2.9-alpine
    container_name: muteeb-caddy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    environment:
      SITE_DOMAIN: ${SITE_DOMAIN:-dev.muteeblabs.uk}
      ACME_EMAIL: ${ACME_EMAIL:-business@muteeblabs.uk}
    volumes:
      - ./deploy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    networks:
      - edge
    depends_on:
      web:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "caddy", "validate", "--config", "/etc/caddy/Caddyfile", "--adapter", "caddyfile"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s

  web:
    build:
      context: .
      target: web
      args:
        VITE_SITE_URL: ${PUBLIC_SITE_URL:-https://dev.muteeblabs.uk}
        VITE_CONTACT_API_URL: ${VITE_CONTACT_API_URL:-/api/contact}
    image: muteeb-portfolio/web:latest
    container_name: muteeb-web
    restart: unless-stopped
    depends_on:
      api:
        condition: service_healthy
    expose:
      - "80"
    networks:
      - edge
      - internal
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s

  api:
    build:
      context: .
      target: api
    image: muteeb-portfolio/api:latest
    container_name: muteeb-api
    restart: unless-stopped
    env_file:
      - path: .env
        required: false
    environment:
      PUBLIC_SITE_URL: ${PUBLIC_SITE_URL:-https://dev.muteeblabs.uk}
      CORS_ORIGINS: ${CORS_ORIGINS:-https://dev.muteeblabs.uk,http://localhost:5173}
      MAIL_BRAND_NAME: ${MAIL_BRAND_NAME:-Muteeb Labs}
      EMAIL_HOST: ${EMAIL_HOST:-}
      EMAIL_PORT: ${EMAIL_PORT:-587}
      EMAIL_USE_TLS: ${EMAIL_USE_TLS:-True}
      EMAIL_USER: ${EMAIL_USER:-}
      EMAIL_PASS: ${EMAIL_PASS:-}
      EMAIL_FROM: ${EMAIL_FROM:-}
      EMAIL_TO: ${EMAIL_TO:-}
      SEND_CLIENT_ACK: ${SEND_CLIENT_ACK:-True}
      MAIL_SIGN_NAME: ${MAIL_SIGN_NAME:-Muteeb Ur Rehman}
      MAIL_SIGN_TITLE: ${MAIL_SIGN_TITLE:-Full Stack Developer & AI Engineer}
      MAIL_REPLY_WINDOW: ${MAIL_REPLY_WINDOW:-usually within one day}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      BOOKING_DB_PATH: /data/bookings.sqlite
      BOOKING_TIMEZONE: ${BOOKING_TIMEZONE:-Asia/Karachi}
      MEETING_LINK: ${MEETING_LINK:-}
      HUBSPOT_ENABLED: ${HUBSPOT_ENABLED:-false}
      HUBSPOT_ACCESS_TOKEN: ${HUBSPOT_ACCESS_TOKEN:-}
      HUBSPOT_PIPELINE_ID: ${HUBSPOT_PIPELINE_ID:-default}
      HUBSPOT_DEAL_STAGE: ${HUBSPOT_DEAL_STAGE:-appointmentscheduled}
      N8N_BOOKING_WEBHOOK_URL: ${N8N_BOOKING_WEBHOOK_URL:-}
      BOOKING_ADMIN_KEY: ${BOOKING_ADMIN_KEY:-}
    volumes:
      - booking_data:/data
    expose:
      - "8000"
    networks:
      - internal
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8000/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 12s

volumes:
  booking_data:
  caddy_data:
  caddy_config:

networks:
  edge:
    driver: bridge
  internal:
    driver: bridge
